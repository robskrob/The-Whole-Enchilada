%main
  = flash[:notice]
  = flash[:error]
  = flash[:alert]
  %h1 home page
  - if current_user
    = button_to('Log Out', session_path(current_user.id), method: :delete, class: 'Account__tab')
  - else
    %h2= link_to('Account', 'sessions/new')
